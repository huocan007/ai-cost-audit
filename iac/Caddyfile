{
    # Caddy 自动申请/续期 SSL；把下面的邮箱换成你自己的
    email admin@acan.ccwu.cc
}

ai.acan.ccwu.cc {
    # 落地页（静态）
    root * /srv/landing
    file_server

    # AI 网关：/v1 反代到 LiteLLM
    handle_path /v1/* {
        reverse_proxy litellm:4000
    }

    # 日志（可选）
    log {
        output stdout
    }
}
