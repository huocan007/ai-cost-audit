# AI Cost Audit 项目总方案与运维手册

> **文档用途**：本项目从 0 到 1 的完整构建记录、架构说明、关键决策、踩坑清单与日常维护 SOP。
> 日后更新落地页 / OG 图 / 定价 / 部署时，先读本文件，照「维护 SOP」操作，避开「踩坑记录」里标记过的雷。
> **最后更新**：2026-07-27 | **维护人**：yican

---

## 一、项目概述

### 1.1 定位
- **一句话**：帮西方开发者/中小企业把 LLM API 账单砍掉 70–90%，用智能路由把请求发到更便宜的模型（DeepSeek / Kimi / Qwen 等），质量不变。
- **起源**：从 "esp32-ai 算力套利" 概念演化而来，最终落定为 **AI 成本优化服务**（AI Cost Audit）。
- **面向市场**：海外（西方客户），非国内渠道。
- **核心卖点**（已重构）：**按数据敏感度在「省钱 ↔ 合规」之间由客户滑动选择**，而非单纯"用中国模型省 90%"（后者与西方数据安全顾虑正面冲突，见 §四合规红线）。

### 1.2 价值主张（对外三句）
1. Same prompts, same quality — lower cost.
2. Route each task to the right model tier.
3. GDPR-aware: you control data residency.

### 1.3 技术栈
| 层 | 选型 | 说明 |
|----|------|------|
| 成本路由 | **LiteLLM 网关** | 客户只改 `base_url`，不改业务代码 |
| CLI 审计工具 | Python (`ai_cost_audit.py`) | 免费开源，算账单节省 |
| 落地页 | 静态 HTML (`index.html`) | 暖白编辑风 + 原创 SVG 视觉 |
| 托管 | VPS + **Nginx** | 网站根目录 `/var/www/html` |
| 加速/SSL | **Cloudflare 代理** | 域名 `ai.acan.ccwu.cc` → VPS `192.3.90.244` |
| 联系通道 | **Web3Forms** | key `e2e23e2b-075c-495a-8ce7-f0ec2b6226ed` |
| 代码托管 | GitHub `huocan007/ai-cost-audit` | 公开仓库 |
| 社媒卡片 | OG 图 `og-image.jpg` (1200×575) | 代码叠字生成 |

---

## 二、项目结构（文件树 + 职责）

```
cost-arbitrage-stack/
├── landing/
│   ├── index.html          # 落地页主文件（暖白编辑风，含 Web3Forms 表单/ROI 计算器/SVG 视觉）
│   └── og-image.jpg        # 社媒分享卡片（1200×575，已上线）
├── tool/
│   └── ai_cost_audit.py    # 免费 CLI 审计工具（PRICING 字典 + audit 命令）
├── README.md               # 仓库说明（克隆地址/用法/示例输出）
├── launch-posts.md         # 发布三连文案（Show HN / PH / Reddit）
├── proposal-template.md    # 客户提案模板
├── PAGE_AUDIT.html         # 专业客户审查报告（13 项 P0/P1/P2 问题）
├── DATA_SECURITY_RESEARCH.md  # 数据驻留/GDPR 合规研究
├── DEPLOY_LANDING.md       # 落地页部署说明
├── REDEPLOY.md             # 重新部署说明
├── IMPROVMX_SETUP.md       # ImprovMX 邮件（备选，未激活）
├── ZOHO_MAIL_SETUP.md      # Zoho 邮件（备选）
└── iac/  backup/           # 基础设施即代码 / 备份
```

**VPS 实际服务目录**：`/root/ai-cost-audit/`（git 拉取）→ 复制 `landing/*` 到 `/var/www/html/`（Nginx 根）。

---

## 三、架构设计

### 3.1 成本路由架构（核心）
```
客户 App
   │  (只改 base_url → 指向网关)
   ▼
AI Cost Gateway (LiteLLM)
   ├─ quality gate / budget cap / BYOK
   ├─ 路由决策：按任务敏感度选模型档
   ▼
┌─────────────────┬─────────────────┬─────────────────┐
│ Cloud Asia       │ Western Cloud   │ Self-hosted    │
│ (DeepSeek/Kimi/  │ (GPT/Claude     │ (开源权重,      │
│  Qwen, 最便宜)    │  美欧, 含 PII)   │  零出口)         │
└─────────────────┴─────────────────┴─────────────────┘
```
- 客户无需改业务代码，只换 `base_url` —— 落地页文案原话："1 swap · change base_url — nothing else"。

### 3.2 数据驻留三档（合规核心）
| 档 | 去哪 | 留存 | 适用 |
|----|------|------|------|
| A 云便宜路由 | Asia 模型 API | 请求过境不留存 | 成本敏感、非敏感负载 |
| B Western 模型 | 美欧 (GPT/Claude) | 留美欧 | 含 PII / 受监管 |
| C 自托管 | 客户自己环境 | 零出口 | 最高敏感 |

**红线**：EU PII 不走 Asia 路由（中国无 GDPR 充分性认定）；美国联邦/国防不接。

---

## 四、关键决策与事实修正（重要）

### 4.1 模型价格（2026-07 已联网核实，**勿用旧价**）
| 模型 | 输入 $/M | 输出 $/M | 备注 |
|------|---------|---------|------|
| DeepSeek V4-Flash | 0.14 | 0.28 | 主力省钱档 |
| Kimi K2.6 | 0.95 | 4.00 | |
| Kimi K3 | 3.00 | 15.00 | 2026-07-17 发布 |
| GPT-4o | 2.50 | 10.00 | |
| GPT-4.1 | 2.00 | 8.00 | |
| Claude Sonnet 4.6 | 3.00 | 15.00 | 即原"Claude 3.5 Sonnet"（命名已过期） |
| Claude Opus 4.8 | 5.00 | 25.00 | 即原"Claude Opus 4"（已退役） |

**标准样例**（50M prompt / 20M completion）：
- GPT-4o：$325 → DeepSeek $12.60（省 96.1%）→ Kimi $127.50（省 60.8%）

### 4.2 命名修正（Anthropic 2026-07 现役线）
- ❌ 旧页面写的 "Claude 3.5 Sonnet" → ✅ **Claude Sonnet 4.6**
- ❌ 旧页面写的 "Claude Opus 4"（退役价 $15/$75）→ ✅ **Claude Opus 4.8**（$5/$25）
- 计算器/下拉/sub2/联系表单全部已改现役名+正确价。

### 4.3 合规红线（**绝不打破**）
- 绝不说"中国 AI 绝对安全"（假话+懂行买家一眼识破）。
- 方向："We show you exactly where every prompt goes. You choose per task."
- 文案须透明 + 给客户控制权（BYOK 默认）。

---

## 五、OG 图片制作流程（v15→v23 迭代）

### 5.1 方法论（已验证最佳实践）
- **AI 生图只做无字底图**（纹理/构图），文字必须**代码叠**（Pillow + Segoe UI Bold，与页面 Inter 近似）才清晰。顶级 SaaS 皆如此。
- 底图源：`C:\Users\yican\Downloads\b2b-saas-og-hero-v2_1785127569.png`（即梦生成，1376×768，暖白+绿网关→橙/蓝/绿三目标+流动点）。
- 生成脚本用托管 venv：`C:\Users\yican\.workbuddy\binaries\python\envs\Default\Scripts\python.exe`，字体 `C:\Windows\Fonts\segoeuib.ttf` / `segoeui.ttf`。
- 输出 1200×575 JPEG quality 92（≈95KB，远低于 200KB 社媒限制）。

### 5.2 最终版（v23）布局
- **左上**：主标题 `Cut 70–90% off your LLM bill`（58px 粗体黑）+ 绿下划线 + 副标题（22px 黑）。
- **左下**：分割线 + 3 条 27px 粗体黑要点（绿点 bullet）。
- **右下**：架构底图，三节点标签（Cloud Asia / Western Cloud / Self-hosted）黑色 14px，**居中**在各自框内。
- **Gateway 圆圈**：**不放文字**（仅留图标）—— 圆圈小+有图标，放字必溢出重叠（见踩坑 #4）。
- **底部**：域名水印 `ai.acan.ccwu.cc`。

### 5.3 节点标签坐标（v23 已校准，勿乱改）
| 节点 | 坐标 (x, y) |
|------|------------|
| Cloud Asia (橙) | (1059, 176) |
| Western Cloud (蓝) | (1059, 278) |
| Self-hosted (绿) | (1059, 386) |

---

## 六、部署与上线（已验证流程）

### 6.1 最终上线状态（2026-07-27 14:07 ✅）
- **URL**：https://ai.acan.ccwu.cc （Cloudflare 代理 + Nginx 源站 HTTP 回源）
- **源站**：VPS `192.3.90.244`，Nginx 监听 80，根目录 `/var/www/html`
- **GitHub**：`huocan007/ai-cost-audit`（公开）

### 6.2 日常更新 SOP（**照此执行，别跳步**）
```bash
# 1. 本地改文件（landing/index.html 或 tool/ai_cost_audit.py 等）
# 2. 提交推送
cd /d/WorkBuddy/日常/cost-arbitrage-stack
git add -A && git commit -m "描述" && git push

# 3. VPS 拉取 + 复制到 Nginx 根目录（⚠️ 必须 cp，不能改 nginx root 指向 /root/）
ssh -p 10022 root@192.3.90.244
cd /root/ai-cost-audit && git pull && cp -r landing/* /var/www/html/

# 4. 若 Cloudflare 缓存没刷新 → CF 面板 Purge Everything
```
⚠️ **OG 图改动后**：除了 git push + cp，还要确认 `og-image.jpg` 文件名严格一致（与 index.html 的 `og:image` meta 一致），否则社媒卡片不更新。

---

## 七、⚠️ 踩坑记录（重点！日后必看）

> 每条都是真金白银踩过的雷。维护时对照检查，避免重蹈覆辙。

### 🔴 坑 #1：Nginx 抢占 80 端口（最致命）
- **现象**：git pull 成功、文件都在，但访问域名显示**旧站**（ACAN Network）。
- **根因**：VPS 上 **Nginx（PID 757）一直占着 80 端口**，之前以为跑的是 `python -m http.server 80`，实际被 Nginx 挤掉。
- **正确做法**：用 Nginx 服务，改 `/etc/nginx/sites-enabled/*` 的 `root` 指向网站目录，**不要**试图用 python http.server 抢 80。

### 🔴 坑 #2：Nginx worker 无权读 /root/ → 404
- **现象**：`sed` 把 nginx root 改成 `/root/ai-cost-audit/landing` 后访问 404。
- **根因**：Nginx 工作进程**不是 root**，没有权限读取 `/root/` 目录下的文件。
- **正确做法**：把落地页 `cp` 到 Nginx 能访问的 `/var/www/html/`，root 保持指向 `/var/www/html`。**不要**让 nginx root 指向 `/root/` 下任何路径。

### 🟡 坑 #3：Cloudflare 缓存导致旧页面不更新
- **现象**：Nginx 配置改好、文件复制好，浏览器仍是旧页面。
- **根因**：Cloudflare 代理缓存了旧响应。
- **解决**：① 浏览器 `Ctrl+Shift+R` 强刷；② CF 面板 → Caching → **Purge Everything**。

### 🟡 坑 #4：OG 节点标签在小圆圈内放两行字必溢出
- **现象**：在 Gateway 圆圈内加 "Smart Router" 副标题，文字溢出到左侧三点、与正文重叠、看不清。
- **根因**：圆圈本身小、内部还有图标，空间不足以放两行字；单标签也易偏/重叠。
- **解决（v23 最终）**：**Gateway 圆圈内不放任何文字**，仅留图标。设计 OG 时节点框要留足放字空间，否则干脆不放字。

### 🟡 坑 #5：git .backup 索引导致 fatal unable to index
- **现象**：`git add -A` 反复报 `fatal: unable to index file '.backup/'`。
- **根因**：目录残留 `.backup/`（不存在、非 submodule、索引中亦无），Git 卡死。
- **解决**：`rm -rf .git && git init` 重新初始化后正常。

### 🟡 坑 #6：虚构/未核实模型价写进工具
- **现象**：`tool/ai_cost_audit.py` 里有 `gpt-5.6-sol`($5/$30)、`claude-opus-4.8` 未核实（注释自标"推测"）。
- **风险**：客户看源码会识破"免费工具"承诺破裂（P0 级信任危机）。
- **解决**：删未核实项；保留项全部联网核实真实 2026-07 公开价（见 §4.1）。**改价前必须先联网核实**，别凭记忆。

### 🟡 坑 #7：README / 链接死链
- **现象**：README 写 `github.com/acan-ai/ai-cost-audit`，真实是 `huocan007`。
- **解决**：全仓库 Grep `acan-ai` 清零，统一改 `huocan007`。**克隆地址必须与 GitHub 实际用户名严格一致**。

### 🟡 坑 #8：argparse `--help` 崩溃
- **现象**：`python ai_cost_audit.py audit --help` 报错（help 串含 `%` 被当格式化符）。
- **根因**：help 字符串里有 `%` 触发 argparse 内部 `%` 格式化。
- **解决**：help 文本中的 `%` 转义或改写。

### 🟡 坑 #9：Segoe UI 不显示 ✦ 等符号
- **现象**：用 `✦` 做 bullet 在 Segoe UI 下显示成方框。
- **解决**：改用 `draw.ellipse` 画绿色圆点做 bullet，不依赖 Unicode 符号字体。

### 🟡 坑 #10：OG PNG 超 200KB 社媒限制
- **现象**：初版 PNG 613KB，社媒卡片可能拒收。
- **解决**：转 JPEG quality 92 → 76–95KB。

### 🟡 坑 #11：Git Bash 中文路径
- **现象**：`cd /d/WorkBuddy/日常/cost-arbitrage-stack` 失败（bash 把中文当乱码）。
- **解决**：用引号 `cd "D:/WorkBuddy/日常/cost-arbitrage-stack"`。

### 🟡 坑 #12：GitHub 浏览器回调被代理拦截
- **现象**：Browser OAuth 回调 127.0.0.1 报 ERR_CONNECTION_REFUSED（疑似代理拦截本地回环）。
- **解决**：改用 Fine-grained PAT（仅限 ai-cost-audit 仓库，Contents 读写，30天有效期）。

### 🟡 坑 #13：Web3Forms 禁止服务端提交
- **现象**：服务端 curl 提交返回 `success:false` + "This method is not allowed. Use our API in client side"。
- **说明**：这是防滥用机制，**非 key 错误**（key 错会返回 invalid access key）。真实验证须在浏览器提交表单。
- **现状**：✅ 已在浏览器验证收信成功（huocan007@aliyun.com 收到 Web3Forms 通知）。

### 🟡 坑 #14：仓库混入了临时脚本
- **现象**：OG 迭代过程中 `og_v19.py`/`og_v20.py`/`og_v21.py` 被 `git add -A` 一起提交进仓库。
- **影响**：不影响功能，但污染仓库。
- **建议**：下次维护时 `rm og_v*.py` 后重新 commit 清理。

---

## 八、获客闸门（待突破）

- **P0 闸门**：上线后 3 个月内 ≥ 3 个合格询盘（qualified inquiry）。
- **发布三连待执行**：Show HN / Product Hunt / Reddit（文案见 `launch-posts.md`，已备）。
- **唯一未验证项**：实际获客效果。

---

## 九、快速检查清单（维护前默念）

- [ ] 改定价前**联网核实** 2026 现行价（§4.1），不凭记忆
- [ ] 克隆地址 / 所有链接 Grep `acan-ai` 清零 → `huocan007`
- [ ] 落地页改完 → git push → VPS `git pull && cp -r landing/* /var/www/html/`
- [ ] OG 图改完 → 文件名严格 `og-image.jpg` → 同步 cp 到 /var/www/html/
- [ ] 上线后 Cloudflare **Purge Everything**
- [ ] Nginx root **永远指向 /var/www/html**，不指向 /root/
- [ ] 合规红线（§4.3）**绝不说"中国 AI 绝对安全"**

---

*文档结束。本文件即项目单一事实源（single source of truth），日后任何架构/流程变更请同步更新本节。*
