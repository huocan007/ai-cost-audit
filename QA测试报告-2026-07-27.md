# QA 点击测试报告 — AI Cost Audit 落地页

**测试日期**：2026-07-27
**测试方式**：以真实访客视角，本地起 HTTP 服务（127.0.0.1:8055）+ curl 验证所有可点击元素（外链可达性、锚点存在性、表单投递、JS 逻辑、CSS 作用域）。
**测试范围**：`landing/index.html`（主落地页）、`landing/cheapest-llms-2026.html`（GEO 列表体页）

---

## 一、主落地页 index.html — 可点击元素清单

| # | 元素 | 类型 | 目标 | 结果 |
|---|------|------|------|------|
| 1 | GitHub ↗（顶部导航） | 外链 | github.com/huocan007/ai-cost-audit | ✅ 200 |
| 2 | Try the free CLI（Hero） | 外链 | 同上 | ✅ 200 |
| 3 | Book a free audit（Hero） | 页内锚点 | `#contact` | ✅ 锚点存在（`<section id="contact">` 第 754 行） |
| 4 | 成本计算器 select / 输入框 | 本地交互 | 本地 JS `calc()` | ✅ 4 个下拉项（GPT-4o/4.1/Sonnet 4.6/Opus 4.8）与价格字典 `P` 完全匹配，无 `undefined` 风险 |
| 5 | Send inquiry →（提交按钮） | Web3Forms AJAX | api.web3forms.com/submit | ⚠️ 见 BUG-1 |
| 6 | Book a free audit（结尾段） | 页内锚点 | `#contact` | ✅ |
| 7 | Try the free CLI（结尾段） | 外链 | GitHub | ✅ 200 |

## 二、列表体页 cheapest-llms-2026.html — 可点击元素清单

| # | 元素 | 目标 | 结果 |
|---|------|------|------|
| 1 | GitHub ↗（导航） | github.com/huocan007/ai-cost-audit | ✅ 200 |
| 2 | Get My Free AI Cost Audit（Hero） | ai.acan.ccwu.cc/#contact | ✅ 锚点存在 |
| 3–11 | 9 条「official ↗」/Securities Times/CloudMart/Tokencost/Anthropic 来源链接 | 各官方源 | ✅ 全部 200 |
| 12 | Get My Free AI Cost Audit（正文结尾） | ai.acan.ccwu.cc/#contact | ✅ |
| 13 | AI Cost Audit 内联链接 | ai.acan.ccwu.cc/ | ✅ 200 |
| 14 | Get My Free AI Cost Audit（sticky 底栏） | ai.acan.ccwu.cc/#contact | ✅ 仅 ≤680px 移动端显示，已验证 CSS 作用域正确（桌面 `display:none`，无泄漏） |

---

## 三、BUG 汇总

### 🔴 BUG-1（P2，阻断性需真人验证）：联系表单无法在沙箱验证真实投递
- **现象**：从沙箱（数据中心 IP）POST `https://api.web3forms.com/submit` 返回 `403`，响应体明确：
  > "This method is not allowed. Use our API in client side or contact support with server IP address (Pro plan is required)"
- **根因**：Web3Forms **设计上拒绝服务端 IP 提交**（防滥用），只允许真实浏览器端（住宅 IP + 正确 Origin）提交。因此**代码本身正确**，真实访客在浏览器点击提交是能成功的——但我无法替你确认「邮件是否真到达你的收件箱」。
- **残留风险（必须真人实测）**：
  1. access_key（`e2e23e2b-…`）的绑定邮箱是否已通过 Web3Forms 验证？未验证则投递失败。
  2. 该 key 的域名白名单是否包含 `ai.acan.ccwu.cc`？（免费档默认允许所有域名，但保险起见确认）
- **结论**：表单 JS 逻辑正确（AJAX、成功/失败均有 UI 反馈、有 honeypot 防机器人）。**需你本人在浏览器实测一次**：打开 `https://ai.acan.ccwu.cc/#contact` → 填写 → 提交 → 查收邮件。

### 🟡 BUG-2（P3，已修复）：联系表单下拉重复选项
- **现象**：`<select id="cmodel">` 中 `GPT-4.1` 出现两次（第 778 与 781 行）。
- **修复**：已删除重复的一条。无害但属数据录入瑕疵。

### 🟢 其余项全部通过
- 所有外链 200、所有锚点存在、计算器字典匹配、sticky CTA 作用域正确、表单成功/失败 UI 完整。

---

## 四、回归测试 SOP（日后改完页面必跑）
1. 本地起服务：`python -m http.server 8055 --bind 127.0.0.1`（在 `landing/` 目录）
2. 逐条核对上方两张清单的「目标」是否仍可达（外链改过 URL 要重测）
3. 表单：沙箱只能确认「代码正确」，真实投递必须浏览器实测
4. 改完记得 `git push` + VPS `git pull && cp -r landing/* /var/www/html/`
